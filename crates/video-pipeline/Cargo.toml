[package]
name = "video-pipeline"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
ffmpeg-next = "8.1.0"

[features]
# See crates/mp4-encoder/Cargo.toml for what this does and why.
static-ffmpeg = ["ffmpeg-next/build"]
